//Bootstrapping...
//var wsServer = new WebSocket("ws://localhost:8088");
var canvas = document.getElementById("mainCanvas");
var context = canvas.getContext('2d');
context.fillStyle = 'red';
context.fillRect(30, 30, 50, 50);
